apiVersion: v1
kind: Pod
metadata:
  name: pod-tol
  labels:
    env: test
spec:
  containers:
  - name: nginx
    image: nginx
    imagePullPolicy: IfNotPresent
  tolerations:
  - key: "disk"
    operator: "Equal"
    value: "ssd"
    effect: "NoSchedule"

